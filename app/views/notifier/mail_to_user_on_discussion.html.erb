<%= @discussion_user%> commented on your
<%= @discussion.discussion_type == "URL" ? "link," : "discussion,"%> <%= @discussion_url %>:<br/>
<%= @comment %>

<br/>
<%= show_discussion_button(@host, @discussion.id).html_safe %>