<% if @emails.nil? %>
  display_flash_message("Please select emails to add.");
<% elsif @added %>
  window.location = "/discussion_groups/<%= @group.id %>";
<% else %>
  display_flash_message("<%= @notice.html_safe %>");
<% end %>